name: WDIO + Cucumber

on:
  workflow_dispatch:
    inputs:
      node:
        description: "Node.js version"
        default: "18"
        required: true
        type: string
  push:
    branches: [ main, master ]
    paths:
      - 'WebdriverIO/WDIO-cucumber/**'
      - '.github/workflows/wdio-cucumber.yml'
  pull_request:
    paths:
      - 'WebdriverIO/WDIO-cucumber/**'
      - '.github/workflows/wdio-cucumber.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ github.event.inputs.node || '20' }}

      - name: Install dependencies
        run: npm --prefix "WebdriverIO/WDIO-cucumber" ci

      - name: Run WDIO tests (Cucumber)
        run: npx --prefix "WebdriverIO/WDIO-cucumber" wdio run "WebdriverIO/WDIO-cucumber/wdio.conf.ts"

      - name: Upload allure-results (raw)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wdio-cucumber-allure-results
          path: "WebdriverIO/WDIO-cucumber/allure-results"
          if-no-files-found: ignore

      - name: Generate Allure report
        if: always()
        run: npx --prefix "WebdriverIO/WDIO-cucumber" allure generate --clean "WebdriverIO/WDIO-cucumber/allure-results" -o "WebdriverIO/WDIO-cucumber/allure-report"

      - name: Upload Allure HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wdio-cucumber-allure-report
          path: "WebdriverIO/WDIO-cucumber/allure-report"
          if-no-files-found: ignore


