name: Cypress + Mocha

on:
  workflow_dispatch:
    inputs:
      node:
        description: "Node.js version"
        default: "18"
        required: true
        type: string
  push:
    branches: [ main, master ]
    paths:
      - 'Cypress/Cypress-mocha/**'
      - '.github/workflows/cypress-mocha.yml'
  pull_request:
    paths:
      - 'Cypress/Cypress-mocha/**'
      - '.github/workflows/cypress-mocha.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ github.event.inputs.node || '20' }}

      - name: Install dependencies
        run: npm --prefix "Cypress/Cypress-mocha" ci

      - name: Run Cypress (with Allure)
        run: npm --prefix "Cypress/Cypress-mocha" run cy:run

      - name: Upload allure-results (raw)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-mocha-allure-results
          path: "Cypress/Cypress-mocha/allure-results"
          if-no-files-found: ignore

      - name: Generate Allure report
        if: always()
        run: npx --prefix "Cypress/Cypress-mocha" allure generate --clean "Cypress/Cypress-mocha/allure-results" -o "Cypress/Cypress-mocha/allure-report"

      - name: Upload Allure HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-mocha-allure-report
          path: "Cypress/Cypress-mocha/allure-report"
          if-no-files-found: ignore


